@model IEnumerable<WebJackpot.Models.Jackpot>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CurrentWin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CurrentTime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CurrentWin)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CurrentTime)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.JackpotID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.JackpotID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.JackpotID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
